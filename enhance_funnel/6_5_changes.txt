gemini -p "You are refactoring enhance_agent to match the architecture of non_tm_sar_agent. Apply the following changes:

1. EVAL FRAMEWORK: Create a BaseEvaluator abstract base class with get_ground_truth_value(row), get_predicted_value(model_output), and filter_rows(df) methods. Create concrete evaluator classes per field. Create run_evaluation.py as a single runner with --fields argparse flag. Do NOT delete existing eval_*.py files.

2. PARSE NARRATIVE: In parse_narrative.py, extract metadata merging logic into a standalone pure function _merge_metadata_sources(). Add a docstring to extract_sample_transaction_table() stating its heuristic rules: a line qualifies if it contains a date pattern AND a dollar amount AND is under 150 characters.

3. INDUCT THIN CLIENT: Refactor induct_by_id.py into a thin client. Move data fetching, DataFrame merging, and the row-by-row processing loop into a new method process_cases(self, case_ids: list) on the agent class in agent.py.

4. FUNNEL ACCOUNT FIELDS: In the output schema and any code that writes to raw_indicator_matches.json, move funnel_account_type, triggered_pattern, and involves_funnel_account_cash_deposits out of matched_indicators and into top-level fields on each case record, at the same level as filed_on_instruments.

Do NOT change any extraction logic, prompts, or business rules." \
@agent.py @induct_by_id.py @parse_narrative.py @llm_helpers.py



gemini -p "Compare llm_helpers.py and llm_helpers_6_5.py. 

Identify any functions, LLM calls, prompts, or logic that exist in llm_helpers_6_5.py but are absent or different in llm_helpers.py. Report only the delta — what is in the old file that the new file does not have. Do NOT make any changes." \
@llm_helpers.py @llm_helpers_6_5.py




gemini -p "Port the following from llm_helpers_6_5.py into llm_helpers.py. Adapt to use the existing _invoke_llm() pattern. Do NOT change anything else in llm_helpers.py:

1. EXTRACT_FUNNEL_ACCOUNT_DETAILS_PROMPT constant
2. _format_funnel_account_details_prompt() helper
3. extract_funnel_account_details() async public function
4. classify_narrative() async public stub" \
@llm_helpers.py @llm_helpers_6_5.py



gemini -p "Search all Python files in this folder. Check if any existing script retrieves or filters case_ids by a given FinCEN tag from BigQuery or from any local JSON/CSV output. Report what you find. If no such script exists, write one called get_case_ids_by_tag.py that queries BigQuery to return all case_ids for a given FinCEN tag, suitable for use as input to induct_by_id.py." \
@**/*.py




gemini -p "Investigate why extract_funnel_account_details() is producing no output in the current pipeline. Trace the full call chain: starting from agent.py where the function is called, through llm_helpers.py where it is defined. Check for: missing await, the function not being called at all, output not being written to the result dict, or _invoke_llm() returning empty. Report findings only, do NOT make changes." \
@agent.py @llm_helpers.py


