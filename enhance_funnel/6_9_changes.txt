gemini -p "Using process_ground_truth.py as a template, write a new script called process_ground_truth_enhance.py that reads ground_truth_sample_6_8.csv and converts it into the same normalized output format produced by process_ground_truth.py. Infer the column mappings from the two files directly. Do not change process_ground_truth.py." \
@process_ground_truth.py @ground_truth_sample_6_8.csv



gemini -p "Modify eval_investigations.py to evaluate the following field mappings against the ground truth file ground_truth_sample_6_8.csv:

1. DEPOSITS: map instrument_flows.deposits.check/cash/eft_wire/p2p/virtual_currency to ground truth columns deposits_check, deposits_cash, deposits_eft_wire, deposits_p2p, deposits_vc

2. DEBITS: map instrument_flows.debits.check/cash/eft_wire/p2p/virtual_currency to ground truth columns debits_check, debits_cash, debits_eft_wire, debits_p2p, debits_vc

3. HRC: map investigations_metadata.hrc.flag to ground truth column HRC. Map investigations_metadata.hrc.jurisdictions (non-empty list = Y) to ground truth column HRC_Jurisdiction. Map HRC_Jurisdiction Y to International column Y.

Match on CASE_ID. For each field output precision, recall, and F1. Print a summary table at the end grouped by field.

Do NOT evaluate investigations_metadata.filed_on. Do NOT modify any extraction logic or prompts." \
@eval_investigations.py @ground_truth_sample_6_8.csv @raw_indicator_matches_by_id.json


gemini -p "Modify check_instruments.py to reflect the current JSON output structure. Use the field mappings in eval_investigations.py as the authoritative reference for how instrument_flows.deposits.*, instrument_flows.debits.*, and investigations_metadata.hrc.* map to output fields. Do NOT change any extraction logic or prompts." \
@check_instruments.py @raw_indicator_matches_by_id.json @eval_investigations.py


gemini -p "Enhance run_evaluation.py and evaluators.py with two changes:

1. MISMATCH DETAIL: In the mismatch report, for each mismatched CASE_ID show three columns: CASE_ID, ground_truth value, and model_output value side by side.

2. FIELD ARGUMENT FIX: The --fields argument currently fails on dot-notation values like deposits.check. Fix the argparse definition so dot-notation field names are accepted without error.

Do NOT change any evaluation logic, scoring, or field mappings." \
@run_evaluation.py @evaluators.py




gemini -p "Refactor the funnel account extraction in llm_helpers.py and agent.py as follows:

1. SPLIT INTO TWO LLM CALLS:

   Call 1 - keep extract_funnel_account_details() but scope it to Introduction and Conclusion sections only. It extracts only triggered_pattern and funnel_account_type. Update _format_funnel_account_details_prompt() to inject only introduction and conclusion sections.

   Call 2 - create a new async function extract_funnel_instrument_flows() with its own system prompt EXTRACT_FUNNEL_INSTRUMENT_FLOWS_PROMPT. It receives Details of Investigation section and sample table block. It extracts instrument_flows with two keys: deposits and debits, each containing check, cash, eft_wire, p2p, virtual_currency as Y/N fields.

2. REWRITE EXTRACT_FUNNEL_INSTRUMENT_FLOWS_PROMPT to match the quality of EXTRACT_INVESTIGATIONS_METADATA_PROMPT. It must include:
   - Exhaustive keyword lists per instrument type matching those in EXTRACT_INVESTIGATIONS_METADATA_PROMPT
   - Explicit deposits vs debits distinction: deposits = funds coming INTO the account (cash deposits, incoming wires, incoming P2P), debits = funds going OUT (ATM withdrawals, outgoing wires, outgoing P2P)
   - A sample transaction table injection block
   - At least 2 few-shot examples showing correct deposits/debits separation
   - BASE ON TEXT ONLY rule

3. WIRE IN agent.py: call both functions, merge results so that triggered_pattern and funnel_account_type come from Call 1, instrument_flows comes from Call 2. Also OR-merge extract_filed_on_from_sample_table results into instrument_flows.deposits the same way it is done in non_tm_sar_agent.

Do NOT modify EXTRACT_FUNNEL_ACCOUNT_DETAILS_PROMPT keyword logic or triggered_pattern/funnel_account_type extraction rules." \
@llm_helpers.py @agent.py




gemini -p "In EXTRACT_FUNNEL_INSTRUMENT_FLOWS_PROMPT in llm_helpers.py, add the following as rule 6 after the existing BASE ON TEXT ONLY rule:

'6. **SUSPICIOUS ACTIVITY SCOPE ONLY**: Flag an instrument as Y only if it is explicitly associated with the suspicious activity being reported. Do NOT flag instruments that appear only in the context of normal, expected, or exculpatory account behavior described by the analyst (e.g., routine transactions used to explain why activity is not suspicious). If an instrument appears in both suspicious and non-suspicious context, flag Y.'

Do not change anything else." \
@llm_helpers.py



gemini -p “In EXTRACT_FUNNEL_INSTRUMENT_FLOWS_PROMPT in llm_helpers.py, add the following as rule 7:

‘7. DIRECTION INDICATORS: The same instrument may appear as both a deposit and a debit. Use directional language to assign correctly: words like credit, incoming, received, deposited indicate deposits; words like debit, outgoing, sent, withdrawn, payment indicate debits. Example: Zelle credits = deposits.p2p Y; Zelle debit = debits.p2p Y. Both can be Y simultaneously.’

Do not change anything else.” @llm_helpers.py

gemini -p “In EXTRACT_FUNNEL_INSTRUMENT_FLOWS_PROMPT in llm_helpers.py, find the eft_wire keyword list and add ‘Remitly’, ‘RMTLY’, ‘remittance’, ‘money transfer service’ to it. Also add an explicit note under the eft_wire definition: ‘Remitly and similar international money transfer services (Remitly, Western Union, MoneyGram) classify as eft_wire regardless of how they are described in the narrative.’

Do not change anything else.” @llm_helpers.py

I need to add both of these right, i didn’t have the language fix for inflows and outflows just yet


gemini -p "In EXTRACT_FUNNEL_INSTRUMENT_FLOWS_PROMPT in llm_helpers.py, replace rule 3 entirely with:

'3. **DEPOSITS vs. DEBITS**: Always analyze from the perspective of the subject account under investigation, not the counterparty.
   * deposits: Funds credited TO the subject account — cash deposited at a branch, incoming wires received, ACH credits posted, incoming P2P payments received (e.g., Zelle credits, incoming CashApp).
   * debits: Funds debited FROM the subject account — cash withdrawals, outgoing wires sent, ACH debits, outgoing P2P payments sent (e.g., Zelle debits, Remitly withdrawals), ATM withdrawals.'

Do not change any other rules." \
@llm_helpers.py



