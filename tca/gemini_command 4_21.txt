Look at agent_tester.ipynb as reference. Create a new Python script 
called induct_tester.py that runs induct() at limit=10 with 
target_fincen_tag=821, and at the end saves the complete Rules 
Hypothesis to a JSON file called rules_hypothesis_limit10.json. 
I'll run this on Vertex AI Workbench.


Before we change any prompts, I want to diagnose two mechanical issues:

1. In agent.py induct() loop: show me exactly how active_fincen_tags 
is built and how it's passed into synthesize_rules(). I want to verify 
that when a narrative has multiple FinCEN tags, each tag's rules are 
being synthesized in isolation and not cross-contaminating each other.

2. In srl_extract.py: confirm that srl_extract() is running only on 
the Introduction and Conclusion sections, not the full narrative. Show 
me the exact input being passed to srl_extract() in the induct() loop.

I want to understand the call structure in agent.py more precisely. 
Is synthesize_rules() called once per narrative (with all active_fincen_tags 
passed together), or is it called once per tag per narrative? 

If it's called once with all tags together, I want to evaluate whether 
we should restructure to call it once per tag in isolation, passing only 
that tag's context each time. Show me the exact loop structure in induct().


I have confirmed cross-contamination in the output — indicators from 
one FinCEN tag are appearing in rules for a different tag. We need to 
restructure agent.py to call synthesize_rules() once per tag per 
narrative, not once per narrative with all tags together.

Redesign the induct() loop so that:
1. For each narrative, iterate over active_fincen_tags individually
2. Each synthesize_rules() call receives only the single current tag 
   (not the full list)
3. Pass only the existing rules for that specific fincen_tag from the 
   rules hypothesis, not the entire hypothesis
4. Merge each tag's result back into the full rules hypothesis before 
   moving to the next tag

Show me the restructured loop in agent.py.


Add a KeyBank high-risk country list to sar_procedures.py as a constant 
called HIGH_RISK_COUNTRIES. The list is:

Afghanistan, Belarus, Central African Republic, Congo (Democratic Republic of),
Cuba, Ethiopia, Iran, Iraq, Libya, Mali, Myanmar, Nicaragua, North Korea,
Russia, Somalia, Sudan, South Sudan, Syria, Ukraine (Crimea Region, Donetsk 
People's Republic, Luhansk People's Republic), Venezuela, Yemen

Then update SYNTHESIZE_RULES_PROMPT with these rules for country handling:
1. If a country name appears in a narrative and it matches HIGH_RISK_COUNTRIES, 
   the indicator should say "transactions involving high-risk foreign 
   jurisdiction" — never the country name itself
2. If a country appears that is NOT on the list (e.g. China, Mexico), it is 
   not an HRC indicator — generalize to "foreign counterparty" or drop it
3. Never include any country name in any indicator under any circumstance

Also update GEMINI.md to mark high-risk country list as completed.



