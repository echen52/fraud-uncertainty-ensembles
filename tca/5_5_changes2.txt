
IF(REGEXP_CONTAINS(t.other_text, r'(?i)tax\s*evasion') AND NOT REGEXP_CONTAINS(t.other_text, r'(?i)payroll\s*tax\s*evasion'), 3001, NULL),




Create a new file called fincen_combo_validator.py in the same directory as fincen_distribution_analyzer2.py.

This program validates that specific known combo cases are correctly counted in multiple codes by querying BigQuery directly at the case level — NOT from the aggregated DataFrame.

Import bigquery from google.cloud and set project = “esa-src-prod-5736”.

Run the following BigQuery query and print results for each check:

CHECK 1 — Find cases where other_text contains both Rapid Movement and Negative News patterns, and verify they appear in both code 9001 and 9002 in the exploded output:

WITH combo_cases AS (
SELECT DISTINCT ITEM_ID
FROM (
SELECT A.ITEM_ID, C.SUS_ACT_OTH as other_text
FROM (
SELECT s.CASE_ID AS ITEM_ID
FROM `esa-src-prod-5736.ACT_SRC_DATA.ACT_CLOUD_IMPL_V_SAR_FORM_STATUSES` s
INNER JOIN (
SELECT case_id, MAX(SNAPSHOT_DT) AS SNAPSHOT_DT
FROM `esa-src-prod-5736.ACT_SRC_DATA.ACT_CLOUD_IMPL_V_SAR_FORM_STATUSES`
WHERE AMENDMENT_OF IS NULL
GROUP BY case_id
) ss ON s.CASE_ID = ss.case_id AND s.SNAPSHOT_DT = ss.SNAPSHOT_DT
WHERE DATE(s.DATE_FILED_ENH) BETWEEN DATE ‘2025-01-01’ AND DATE ‘2025-12-31’
) A
LEFT JOIN `esa-prod-7486.ESA_MART_AML_VIEWS.ACT_SAR_BV` C ON A.ITEM_ID = C.CASE_ID
)
WHERE REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’)
AND REGEXP_CONTAINS(other_text, r’(?i)neg[a-z]*\s*news’)
)
SELECT COUNT(*) as combo_case_count FROM combo_cases

Print: “Cases with both Rapid Movement AND Negative News in other_text: {count}”
Print: “These cases should appear in BOTH 9001 and 9002 counts”
Print: “9001 count increase from v1 to v2: 91”
Print: “9002 count increase from v1 to v2: 55”
Print: “If combo_case_count is between 55 and 91, combo explosion is working correctly”

CHECK 2 — Same pattern for Marijuana + Negative News combos:
Replace the WHERE clause patterns with marijuana and negative news patterns.
Print the count and expected codes (9005 and 9002).

CHECK 3 — Same pattern for Drug Trafficking + Rapid Movement combos:
Replace patterns with drug trafficking and rapid movement.
Print the count and expected codes (9004 and 9001).

Print a final summary:
“VALIDATION COMPLETE — combo case counts are consistent with observed count increases in v2”
or
“WARNING — combo counts do not match expected increases”






In fincen_distribution_analyzer2.py, in the exploded_codes CTE, find the FRAUD ARRAY block (around line 108-118).

Find this exact line:
IF(REGEXP_CONTAINS(t.other_text, r’(?i)tax\s*evasion’) AND NOT REGEXP_CONTAINS(t.other_text, r’(?i)payroll\s*tax\s*evasion’), 3001, NULL),

Replace with:
IF(REGEXP_CONTAINS(t.other_text, r’(?i)tax\s*evasion’), 3001, NULL),

Rationale: Both 3001 and 3002 must now evaluate independently. A string containing “tax evasion and payroll tax evasion” must produce BOTH 3001 and 3002 rows. The AND NOT exclusion was preventing this. The 3004 bucket’s exclusion logic already correctly excludes cases that match tax evasion, so removing AND NOT from 3001 does not affect the 3004 bucket.

Also update the FRAUD assertion in **main** (around line 258) from:
assert original_fraud_count == new_fraud_count

To:
assert new_fraud_count >= original_fraud_count, f”FRAUD count error: cases were lost”
print(f”FRAUD combo inflation: {new_fraud_count - original_fraud_count} additional assignments from multi-description cases”)

Do not change anything else.






In fincen_distribution_analyzer2.py, find and replace the following EXACT line (around line 112):

FIND THIS EXACT TEXT:
IF(REGEXP_CONTAINS(t.other_text, r'(?i)tax\s*evasion') AND NOT REGEXP_CONTAINS(t.other_text, r'(?i)payroll\s*tax\s*evasion'), 3001, NULL),

REPLACE WITH THIS EXACT TEXT:
IF(REGEXP_CONTAINS(t.other_text, r'(?i)tax\s*evasion'), 3001, NULL),

This is a one-line change. Do not modify any other line in the file. Do not rewrite or reformat any surrounding code. Only this single line changes.



In fincen_distribution_analyzer2.py, find and replace the following EXACT line (around line 112):

FIND THIS EXACT TEXT:
IF(REGEXP_CONTAINS(t.other_text, r’(?i)tax\s*evasion’), 3001, NULL),

REPLACE WITH THIS EXACT TEXT:
IF(REGEXP_CONTAINS(t.other_text, r’(?i)(and|&|/)\s*tax\s*evasion|tax\s*evasion\s*(and|&|/)’), 3001, NULL),

Rationale: This pattern only fires 3001 when “tax evasion” appears in a combo context — i.e. preceded or followed by “and”, “&”, or “/”. This correctly handles strings like “tax evasion and payroll tax evasion” (fires both 3001 and 3002) while leaving pure “payroll tax evasion” strings to only fire 3002.

This is a one-line change only. Do not modify any other line in the file.





In fincen_distribution_analyzer2.py, find and replace the following line (around line 112). Make the change directly in the file editor, do not copy-paste from this prompt — type it manually to avoid encoding issues.

The line should read exactly:

IF(REGEXP_CONTAINS(t.other_text, r’(?i)(and|/)\s*tax\s*evasion|tax\s*evasion\s*(and|/)’), 3001, NULL),

Key points:

- The pattern uses only “and” and “/” as combo delimiters — no ampersand character at all
- This avoids any Unicode encoding issues with special characters
- Only fires 3001 when “tax evasion” appears next to “and” or “/” in the string
- Do not copy-paste the pattern from this file — type it manually in the editor to ensure pure ASCII characters

This is a one-line change only. Do not modify any other line.




IF(REGEXP_CONTAINS(t.other_text, r'(?i)(and|/)\s*tax\s*evasion|tax\s*evasion\s*(and|/)'), 3001, NULL),


