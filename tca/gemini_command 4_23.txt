The files ffiec_appendix_f.json and ffiec_appendix_g.json are already 
in tca/data/. 

Extend sar_procedures.py with the following additions only — do not 
modify any existing functions:

1. Add HIGH_RISK_COUNTRIES constant — a Python set of country name 
   strings. The list is in GEMINI.md under High-Risk Country List.

2. Add load_ffiec_appendix_f() — loads tca/data/ffiec_appendix_f.json 
   at module level, returns the parsed JSON object.

3. Add load_ffiec_appendix_g() — loads tca/data/ffiec_appendix_g.json 
   at module level, returns the parsed JSON object.

4. Add get_indicator_vocabulary() — returns a single flat list of dicts, 
   one per indicator, combining all indicators from sar_procedure.json, 
   ffiec_appendix_f.json, and ffiec_appendix_g.json. Each dict must have 
   exactly three fields: indicator_id, text, source.

Do not modify any existing functions. Show me the full updated 
sar_procedures.py when done.


Now rewrite llm_helpers.py. Replace extract_narrative_structure() and 
synthesize_rules() with a single new function match_indicators().

match_indicators() takes:
- narrative_sections: dict (output of parse_narrative)
- vocabulary: list of dicts (output of get_indicator_vocabulary)
- spacy_frames: dict (output of srl_extract)

It makes one LLM call with a short focused prompt that:
- Provides the narrative Introduction and Conclusion sections as primary 
  reading material, Details of Investigation as secondary
- Provides the full indicator vocabulary as a numbered reference list 
  with indicator_id and text
- Instructs the model to return ONLY a JSON list of matches in this format:
  [{"indicator_id": "...", "evidence_quote": "..."}]
- Evidence quotes must be one sentence maximum, no proper nouns, no 
  business names, no country names
- The model must only match indicators from the provided vocabulary — 
  it must not invent new ones
- Return format is a raw JSON list only — no preamble, no explanation

Keep classify_narrative() stubbed if it exists. Do not add anything else.
Show me the full updated llm_helpers.py when done.


The prompt looks good. One fix needed before approving: the example 
indicator_id "KBP_3_e" is not a valid format from our vocabulary. 
Replace both example entries with real indicator_ids from the actual 
vocabulary files:
- Use "FFI_F_FT_004" for the funds transfer example
- Use "FFI_F_CSI_001" for the identification example

Then apply the changes to llm_helpers.py.



Now rewrite the induct() method in agent.py to implement the v2 
two-phase architecture from GEMINI.md. 

Phase 1 loop (per narrative):
1. Build active_fincen_tags from row data (keep existing logic)
2. Call parse_narrative() — keep existing call
3. Call srl_extract() — keep existing call  
4. Call get_indicator_vocabulary() from sar_procedures.py — new
5. Call match_indicators() from llm_helpers.py — new, single LLM call
6. Append one record to raw_indicator_matches.json:
   {
     "case_id": case_id,
     "narrative_date": date from row,
     "active_fincen_tags": active_fincen_tags,
     "suspicious_total": pre_parse suspicious_total,
     "review_period": pre_parse review_period,
     "spacy_frames": frames,
     "matched_indicators": result from match_indicators()
   }

Phase 1 only for now — do not build consolidate_hypothesis() yet.

raw_indicator_matches.json should be append-only — if the file already 
exists, load it and append. If not, create it.

Remove all v1 logic: extract_narrative_structure(), synthesize_rules(), 
rules hypothesis mid-stream merging. 

Return a summary dict with narratives_processed and output file path.
Show me the updated induct() method when done.

