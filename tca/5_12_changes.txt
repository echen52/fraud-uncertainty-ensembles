gemini "Look at get_fincen_tag() and how its output is currently merged into the induct() pipeline as context. Then look at agent.py to understand the per-case_id JSON structure that gets built. Based on that pattern, propose where and how to merge the following fields as additional context fields (not indicators, not spaCy items) onto each case in induct(): CASE_TYPE, CASE_SOURCE, SAR_AMT, CASE_CREATE_DT, CASE_CLOSED_DT, FILINGINSTITUTIONNOTE, SAR_FORM_STATUS, SARMON_SOURCE_DISPOSITION. Do not modify indicator logic. Do not modify spaCy logic. Only propose the merge pattern."
