In the `unnested_tags` CTE, the CASE statement that assigns `other_text` is missing mappings for several typologies that have corresponding `_OTH` columns in `base`. Update the CASE statement to add the following mappings:

WHEN field_name = 'CYBER' THEN base.CYBER_OTH
WHEN field_name = 'GAMING' THEN base.GAMING_OTH
WHEN field_name = 'IDNTFCTN' THEN base.DOC_OTH
WHEN field_name = 'INSURANCE' THEN base.INSURANCE_OTH
WHEN field_name = 'MORTGAGE' THEN base.MORT_OTH
WHEN field_name = 'SFO' THEN base.SFO_OTH
WHEN field_name = 'OTH_CATGRY' THEN base.OTH_CATGRY

Also remove the STRUCT('INST_TYPE' AS field_name, SPLIT(INST_TYPE_OTH, ',') as codes) entry from the UNNEST array entirely, and remove the WHEN field_name = 'INST_TYPE' THEN base.INST_TYPE_OTH line from the CASE statement.

Do not change anything else.







In fincen_distribution_analyzer.py, after the existing query logic, add a new function called `remap_other_codes` that takes a DataFrame (the output of `get_fincen_tag_distribution`) and applies regex-based remapping to rows where the code ends in '99' (Other codes).

For the FRAUD typology, rows currently coded 3999 need to be re-evaluated using the free-text in the `other_descriptions` column. Apply the following regex patterns independently and in parallel against `other_descriptions`. A single row may match multiple patterns and should receive all matching codes as a comma-delimited string (same format as existing typology fields). If no pattern matches, the row keeps code 3999.

Regex rules for FRAUD / 3999:
1. If other_descriptions matches (?i)pig\s*butcher → assign code 3003
2. If other_descriptions matches (?i)payroll\s*tax\s*evasion → assign code 3002
3. If other_descriptions matches (?i)(?<!payroll\s)tax\s*evasion → assign code 3001

Important implementation requirements:
- All patterns must be evaluated independently against the original other_descriptions string. Do not chain or sequence them.
- A single row can match multiple patterns simultaneously. Collect all matched codes into a list and join as a comma-delimited string (e.g. "3001,3002").
- The negative lookahead (?<!payroll\s) on the tax evasion pattern prevents it from double-firing on strings already matched by the payroll tax evasion pattern.
- Rows matching at least one pattern should have their code column replaced with the new comma-delimited code(s).
- Rows matching no pattern remain coded as 3999.
- This function must be designed to be extensible: additional typologies and their regex rules will be added in future iterations using the same parallel multi-match pattern.

Do not change the existing `get_fincen_tag_distribution` function. Call `remap_other_codes` on the resulting DataFrame in the __main__ block before saving to CSV.








The `remap_other_codes` function has three bugs that need to be fixed. Do not rewrite the overall structure — fix only the following:

BUG 1 — Indentation error in get_new_codes:
The `matched_codes = []` line and everything below it is incorrectly indented inside the `if not isinstance` block. Fix the indentation so that `matched_codes = []` and the for loop execute for all valid string inputs, not just invalid ones.

BUG 2 — Wrong row mutation logic:
The current line 207 replaces the code value of the existing 3999 row with a comma-joined string. This is wrong. The function must instead:

- For each row where code == original_code (e.g. 3999), evaluate all regex patterns independently against other_descriptions.
- For each new code matched (e.g. 3001, 3002, 3003), create a brand new row in the DataFrame. Each new row has the same typology and other_descriptions as the original, code = the new integer code (not a string), distinct_sar_count = the number of distinct cases whose other_descriptions matched that specific pattern, and Description = the description for that new code (can be left blank or ‘Remapped from Other’ for now).
- The original 3999 row must remain but its distinct_sar_count must be reduced by the number of cases successfully reclassified to any new code. A case that matches two patterns is removed from 3999 once but counted in both new code rows.
- Cases matching no pattern remain under 3999.

BUG 3 — Type mismatch on new code values:
New code values (e.g. ‘3003’, ‘3002’, ‘3001’) are currently strings. They must be integers to match the type of all other code values in the DataFrame.

Do not change the remap_rules dictionary structure, the regex patterns, or any other part of the file.









Update the `remap_other_codes` function with the following three changes. Do not change anything else.

CHANGE 1 — Fix overlapping match between 3001 and 3002:
The negative lookahead pattern for 3001 is not correctly preventing overlap with 3002. Replace the current pattern logic for tax evasion and payroll tax evasion with explicit sequential precedence for these two codes only:
- First check if other_descriptions matches (?i)payroll\s*tax\s*evasion → assign 3002 only
- Only if that does NOT match, then check if other_descriptions matches (?i)tax\s*evasion → assign 3001
This means a string containing "payroll tax evasion" will ONLY produce code 3002, never 3001.
All other patterns (e.g. pig butchering) remain fully independent parallel matches unaffected by this precedence rule.

CHANGE 2 — Add code 3004 bucket for described but unmatched 3999 rows:
After all regex patterns have been evaluated, split the remaining 3999 rows into two buckets:

Bucket 1 — Code 3004, Description = 'Other Fraud — Specified':
Cases where other_descriptions is not null and not blank (after stripping whitespace), but matched none of the regex patterns. distinct_sar_count = number of such cases. other_descriptions = aggregated descriptions of all these cases joined by '; '.

Bucket 2 — Code 3999, Description = '(3999)Other':
Cases where other_descriptions is null or blank. These are truly unspecified. distinct_sar_count = number of such cases. Leave all other fields unchanged.

3004 must be an integer, consistent with all other code values in the DataFrame.

CHANGE 3 — Verify the total count is preserved:
After remapping, the sum of distinct_sar_count across codes 3001 + 3002 + 3003 + 3004 + 3999 for typology FRAUD must equal the original distinct_sar_count of the 3999 row before remapping (267). Add an assertion or print statement to verify this after remapping.

Do not change the remap_rules dictionary structure or any other part of the file.





The `remap_other_codes` Python function cannot correctly assign distinct_sar_count to new codes because it operates on pre-aggregated data. The remapping logic must move into the BigQuery SQL query inside `get_fincen_tag_distribution`, where it can operate at the case level before aggregation.

Make the following changes:

CHANGE 1 — Add a remapped_code column to the final SELECT in the query:
In the final SELECT statement (currently selecting field_name, code, Description, COUNT, STRING_AGG), add a new computed column called `remapped_code` using a CASE expression. This column applies regex remapping rules to the other_text field at the row level, before aggregation. The logic is:

For FRAUD typology only (field_name = ‘FRAUD’), when code matches r’99$’:

- If REGEXP_CONTAINS(other_text, r’(?i)pig\s*butcher’) → remapped_code = 3003
- Else if REGEXP_CONTAINS(other_text, r’(?i)payroll\s*tax\s*evasion’) → remapped_code = 3002
- Else if REGEXP_CONTAINS(other_text, r’(?i)tax\s*evasion’) → remapped_code = 3001
- Else if other_text IS NOT NULL AND TRIM(other_text) != ‘’ → remapped_code = 3004
- Else → remapped_code = 3999 (null or blank description, truly unspecified)

For all other typologies and non-Other codes → remapped_code = code (no change)

Note: payroll tax evasion must be checked BEFORE tax evasion (sequential for these two only) to prevent overlap. All other patterns are independent.

CHANGE 2 — Group by remapped_code instead of code in the final SELECT:
Change GROUP BY 1, 2, 3 to group by field_name, remapped_code, and Description. The COUNT(DISTINCT ITEM_ID) will then correctly reflect case-level counts per remapped code.

CHANGE 3 — Join remapped_code back to lookup_tbl for Description:
After computing remapped_code, left join to lookup_tbl on remapped_code to retrieve the correct Description for new codes. For codes 3001, 3002, 3003, 3004 use these descriptions:

- 3001: ‘Tax Evasion’
- 3002: ‘Payroll Tax Evasion’
- 3003: ‘Pig Butchering’
- 3004: ‘Other Fraud — Specified’
- 3999: ‘(3999)Other’
  These can be hardcoded in a CASE expression for Description if the lookup table does not yet contain these new codes.

CHANGE 4 — Remove the remap_other_codes Python function entirely and remove its call from the **main** block. The remapping is now handled fully in SQL.

Do not change any other part of the query or file.






In the **main** block, after remapped_df is produced, add the following validation print statements. Do not change anything else.

1. Filter remapped_df to typology == ‘FRAUD’ and code == 3002. From the other_descriptions string of that row, count how many semicolon-delimited entries contain ‘tax evasion’ (case-insensitive) but do NOT contain ‘payroll’ (case-insensitive). Print the result as:

“3002 validation — entries containing ‘tax evasion’ without ‘payroll’: {count}”

1. Filter remapped_df to typology == ‘FRAUD’ and code == 3001. From the other_descriptions string of that row, count how many semicolon-delimited entries contain ‘payroll’ (case-insensitive). Print the result as:

“3001 validation — entries containing ‘payroll’: {count}”

Both counts should be 0 if the split is clean.









Update the `get_fincen_tag_distribution` function to add remapping rules for three additional typologies: MONEY_LAUNDERING (8999), OTH_SUS_ACTIVITY (9999), and STRCTNG (1999). Follow the exact same pattern already implemented for FRAUD (3999) in the remapped CTE.

In the CASE statement inside the remapped CTE, add the following blocks. All patterns are independent parallel matches unless noted. All new code values are integers.

— MONEY_LAUNDERING / 8999 —
WHEN field_name = ‘MONEY_LAUNDERING’ AND REGEXP_CONTAINS(code, r’99$’) THEN
CASE
WHEN REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’) THEN 8001
ELSE 8999
END

Note: The broad regex r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’ is intentional to capture all misspellings of “Rapid Movement of Funds” seen in the data (Rapdi, Rappid, Rapit, Rapid Movment, Mvoement, Movemetn, rapid movement fund, rapid movement of fuds, Rapid Mmovement, etc.)

For 8999 remaining rows: same split as FRAUD — rows with non-null non-blank other_text that match no pattern → 8004 ‘Other Money Laundering — Specified’. Rows with null/blank other_text → stay 8999.

— OTH_SUS_ACTIVITY / 9999 —
WHEN field_name = ‘OTH_SUS_ACTIVITY’ AND REGEXP_CONTAINS(code, r’99$’) THEN
CASE
WHEN REGEXP_CONTAINS(other_text, r’(?i)ocse|online\s*child\s*sexual’) THEN 9011
WHEN REGEXP_CONTAINS(other_text, r’(?i)wildlife\s*traff’) THEN 9010
WHEN REGEXP_CONTAINS(other_text, r’(?i)(russia\s*)?(export\s*(evasion|restriction))’) THEN 9009
WHEN REGEXP_CONTAINS(other_text, r’(?i)elder\s*fin’) THEN 9008
WHEN REGEXP_CONTAINS(other_text, r’(?i)payroll\s*tax\s*evasion’) THEN 9007
WHEN REGEXP_CONTAINS(other_text, r’(?i)tax\s*evasion’) THEN 9007
WHEN REGEXP_CONTAINS(other_text, r’(?i)human\s*traff’) THEN 9006
WHEN REGEXP_CONTAINS(other_text, r’(?i)mari[jh]?[iau]+[an]+’) THEN 9005
WHEN REGEXP_CONTAINS(other_text, r’(?i)drug\s*traff’) THEN 9004
WHEN REGEXP_CONTAINS(other_text, r’(?i)(law\s*enf|le\s*aware|law\s*ea)’) THEN 9003
WHEN REGEXP_CONTAINS(other_text, r’(?i)neg[a-z]*\s*news’) THEN 9002
WHEN REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’) THEN 9001
ELSE 9999
END

Notes on OTH_SUS_ACTIVITY ordering:

- OCSE and Wildlife Trafficking check first as they are highly specific.
- Payroll Tax Evasion checks before Tax Evasion (same sequential rule as FRAUD).
- Human Trafficking checks before Drug Trafficking to avoid any overlap.
- Rapid Movement of Funds checks last among the named codes because it is the highest volume and broadest pattern.
- Combo entries (e.g. “Rapid Movement of Funds & Negative News”, “Drug Trafficking/Negative News/Rapid movement fund”) will be assigned to whichever pattern fires first in this CASE. Multi-code handling for combos is a known deferred item.

For 9999 remaining rows: rows with non-null non-blank other_text that match no pattern → 9004 ‘Other Suspicious Activity — Specified’. Rows with null/blank other_text → stay 9999.

— STRCTNG / 1999 —
WHEN field_name = ‘STRCTNG’ AND REGEXP_CONTAINS(code, r’99$’) THEN
CASE
WHEN REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’) THEN 115
ELSE 1999
END

For 1999 remaining rows: same split — non-null non-blank unmatched → new code for ‘Other Structuring — Specified’ (use 116 if available). Rows with null/blank → stay 1999.

— DESCRIPTIONS for new codes —
Add the following to the Description CASE statement:
WHEN 8001 THEN ‘Rapid Movement of Funds’
WHEN 8004 THEN ‘Other Money Laundering — Specified’
WHEN 9001 THEN ‘Rapid Movement of Funds’
WHEN 9002 THEN ‘Negative News’
WHEN 9003 THEN ‘Law Enforcement Awareness’
WHEN 9004 THEN ‘Drug Trafficking’
WHEN 9005 THEN ‘Marijuana’
WHEN 9006 THEN ‘Human Trafficking’
WHEN 9007 THEN ‘Tax Evasion’
WHEN 9008 THEN ‘Elder Financial Exploitation’
WHEN 9009 THEN ‘Export Evasion’
WHEN 9010 THEN ‘Wildlife Trafficking’
WHEN 9011 THEN ‘Online Child Sexual Exploitation’
WHEN 115 THEN ‘Rapid Movement of Funds’
WHEN 116 THEN ‘Other Structuring — Specified’

Also add validation print statements in **main** for each new typology block, verifying that the sum of distinct_sar_count across all new codes plus remaining Other code equals the original Other code count (42 for 8999, 4397 for 9999, 4 for 1999).

Do not change any other part of the file.










Create a new file called fincen_distribution_analyzer2.py. It should be identical to fincen_distribution_analyzer.py with one architectural change: replace the `remapped` CTE with a new `exploded_codes` CTE that supports multi-code assignment for combo descriptions (e.g. “Rapid Movement of Funds & Negative News” should produce two rows, one for each matched code).

The new architecture works as follows:

STEP 1 — Replace the `remapped` CTE with `exploded_codes`:

Instead of a single nested CASE statement that returns one code per row, each row in `unnested_tags` is evaluated against ALL applicable patterns independently. For each pattern that matches, a separate output row is produced with that code. A single input row can therefore produce multiple output rows.

Implement this using UNNEST of an inline ARRAY of STRUCTs. For each typology’s Other code, build an array of candidate codes using IF(REGEXP_CONTAINS(other_text, pattern), candidate_code, NULL), then UNNEST the array and filter out NULLs. Each non-null element becomes its own row.

The structure should look like this (BigQuery SQL):

exploded_codes AS (
SELECT
ITEM_ID,
CASE_GRP,
field_name,
other_text,
matched_code,
lookup_tbl.Description AS original_description
FROM unnested_tags,
UNNEST(
CASE
WHEN field_name = ‘FRAUD’ AND REGEXP_CONTAINS(code, r’99$’) THEN
ARRAY(
SELECT c FROM UNNEST([
IF(REGEXP_CONTAINS(other_text, r’(?i)pig\s*butcher’), 3003, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)payroll\s*tax\s*evasion’), 3002, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)(?<!payroll\s)tax\s*evasion’), 3001, NULL),
IF(other_text IS NOT NULL AND TRIM(other_text) != ‘’ AND
NOT REGEXP_CONTAINS(other_text, r’(?i)pig\s*butcher’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)tax\s*evasion’), 3004, NULL),
IF(other_text IS NULL OR TRIM(other_text) = ‘’, 3999, NULL)
]) AS c WHERE c IS NOT NULL
)
WHEN field_name = ‘MONEY_LAUNDERING’ AND REGEXP_CONTAINS(code, r’99$’) THEN
ARRAY(
SELECT c FROM UNNEST([
IF(REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 8001, NULL),
IF(other_text IS NOT NULL AND TRIM(other_text) != ‘’ AND
NOT REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 8004, NULL),
IF(other_text IS NULL OR TRIM(other_text) = ‘’, 8999, NULL)
]) AS c WHERE c IS NOT NULL
)
WHEN field_name = ‘OTH_SUS_ACTIVITY’ AND REGEXP_CONTAINS(code, r’99$’) THEN
ARRAY(
SELECT c FROM UNNEST([
IF(REGEXP_CONTAINS(other_text, r’(?i)ocse|online\s*child\s*sexual’), 9011, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)wildlife\s*traff’), 9010, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)(russia\s*)?(export\s*(evasion|restriction))’), 9009, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)elder\s*fin’), 9008, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)payroll\s*tax\s*evasion’), 9007, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)(?<!payroll\s)tax\s*evasion’), 9007, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)human\s*traff’), 9006, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)mari[jh]?[iau]+[an]+’), 9005, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)drug\s*traff’), 9004, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)(law\s*enf|le\s*aware|law\s*ea)’), 9003, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)neg[a-z]*\s*news’), 9002, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 9001, NULL),
IF(other_text IS NOT NULL AND TRIM(other_text) != ‘’ AND
NOT REGEXP_CONTAINS(other_text, r’(?i)ocse|online\s*child\s*sexual’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)wildlife\s*traff’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)(russia\s*)?(export\s*(evasion|restriction))’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)elder\s*fin’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)tax\s*evasion’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)human\s*traff’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)mari[jh]?[iau]+[an]+’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)drug\s*traff’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)(law\s*enf|le\s*aware|law\s*ea)’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)neg[a-z]*\s*news’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 9004, NULL),
IF(other_text IS NULL OR TRIM(other_text) = ‘’, 9999, NULL)
]) AS c WHERE c IS NOT NULL
)
WHEN field_name = ‘STRCTNG’ AND REGEXP_CONTAINS(code, r’99$’) THEN
ARRAY(
SELECT c FROM UNNEST([
IF(REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 115, NULL),
IF(other_text IS NOT NULL AND TRIM(other_text) != ‘’ AND
NOT REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 116, NULL),
IF(other_text IS NULL OR TRIM(other_text) = ‘’, 1999, NULL)
]) AS c WHERE c IS NOT NULL
)
ELSE ARRAY[CAST(code AS INT64)]
END
) AS matched_code
LEFT JOIN lookup_tbl ON lookup_tbl.FACTOR_CODE = CAST(matched_code AS STRING)
)

STEP 2 — Update the final SELECT to use exploded_codes instead of remapped:

- Replace FROM remapped with FROM exploded_codes
- Replace remapped_code with matched_code throughout
- Keep the Description CASE statement identical to fincen_distribution_analyzer.py
- Keep COUNT(DISTINCT ITEM_ID), STRING_AGG, WHERE, GROUP BY, and ORDER BY identical

STEP 3 — Update the **main** block:

- Keep all existing validation print statements and assertions identical to fincen_distribution_analyzer.py
- Change the output filename to fincen_tag_distribution_2025_remapped_v2.csv

Do not change anything else. The goal is minimal diff from the original file — only the remapped CTE is replaced by exploded_codes and the final SELECT updated accordingly.







In fincen_distribution_analyzer2.py, the `remapped` CTE (lines 96-139) must be completely replaced. The current implementation uses a nested CASE statement that returns exactly one code per row. This is wrong. Replace it entirely with the following exact SQL CTE.

Delete everything from line 96 (`remapped AS (`) through line 139 (the closing `)`) and replace with this exact code:

exploded_codes AS (
SELECT
ITEM_ID,
CASE_GRP,
field_name,
other_text,
matched_code
FROM unnested_tags,
UNNEST(
CASE
WHEN field_name = ‘FRAUD’ AND REGEXP_CONTAINS(code, r’99$’) THEN
ARRAY(
SELECT c FROM UNNEST([
IF(REGEXP_CONTAINS(other_text, r’(?i)pig\s*butcher’), 3003, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)payroll\s*tax\s*evasion’), 3002, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)tax\s*evasion’) AND NOT REGEXP_CONTAINS(other_text, r’(?i)payroll\s*tax\s*evasion’), 3001, NULL),
IF(other_text IS NOT NULL AND TRIM(other_text) != ‘’ AND
NOT REGEXP_CONTAINS(other_text, r’(?i)pig\s*butcher’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)tax\s*evasion’), 3004, NULL),
IF(other_text IS NULL OR TRIM(other_text) = ‘’, 3999, NULL)
]) AS c WHERE c IS NOT NULL
)
WHEN field_name = ‘MONEY_LAUNDERING’ AND REGEXP_CONTAINS(code, r’99$’) THEN
ARRAY(
SELECT c FROM UNNEST([
IF(REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 8001, NULL),
IF(other_text IS NOT NULL AND TRIM(other_text) != ‘’ AND
NOT REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 8004, NULL),
IF(other_text IS NULL OR TRIM(other_text) = ‘’, 8999, NULL)
]) AS c WHERE c IS NOT NULL
)
WHEN field_name = ‘OTH_SUS_ACTIVITY’ AND REGEXP_CONTAINS(code, r’99$’) THEN
ARRAY(
SELECT c FROM UNNEST([
IF(REGEXP_CONTAINS(other_text, r’(?i)ocse|online\s*child\s*sexual’), 9011, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)wildlife\s*traff’), 9010, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)(russia\s*)?(export\s*(evasion|restriction))’), 9009, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)elder\s*fin’), 9008, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)payroll\s*tax\s*evasion’), 9007, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)tax\s*evasion’) AND NOT REGEXP_CONTAINS(other_text, r’(?i)payroll\s*tax\s*evasion’), 9007, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)human\s*traff’), 9006, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)mari[jh]?[iau]+[an]+’), 9005, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)drug\s*traff’), 9004, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)(law\s*enf|le\s*aware|law\s*ea)’), 9003, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)neg[a-z]*\s*news’), 9002, NULL),
IF(REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 9001, NULL),
IF(other_text IS NOT NULL AND TRIM(other_text) != ‘’ AND
NOT REGEXP_CONTAINS(other_text, r’(?i)ocse|online\s*child\s*sexual’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)wildlife\s*traff’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)(russia\s*)?(export\s*(evasion|restriction))’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)elder\s*fin’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)tax\s*evasion’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)human\s*traff’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)mari[jh]?[iau]+[an]+’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)drug\s*traff’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)(law\s*enf|le\s*aware|law\s*ea)’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)neg[a-z]*\s*news’) AND
NOT REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 9004, NULL),
IF(other_text IS NULL OR TRIM(other_text) = ‘’, 9999, NULL)
]) AS c WHERE c IS NOT NULL
)
WHEN field_name = ‘STRCTNG’ AND REGEXP_CONTAINS(code, r’99$’) THEN
ARRAY(
SELECT c FROM UNNEST([
IF(REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 115, NULL),
IF(other_text IS NOT NULL AND TRIM(other_text) != ‘’ AND
NOT REGEXP_CONTAINS(other_text, r’(?i)rap[a-z]*\s*m[oa]?[a-z]*\s*(of\s*)?fund’), 116, NULL),
IF(other_text IS NULL OR TRIM(other_text) = ‘’, 1999, NULL)
]) AS c WHERE c IS NOT NULL
)
ELSE ARRAY[CAST(code AS INT64)]
END
) AS matched_code
)

Then update the final SELECT (currently line 142) to:

- Change FROM remapped to FROM exploded_codes
- Change remapped_code to matched_code everywhere in the SELECT and GROUP BY
- Keep all other SELECT fields, WHERE clause, GROUP BY, and ORDER BY identical

The key difference from the current implementation: each input row from unnested_tags can now produce MULTIPLE output rows in exploded_codes — one per matched pattern. A case with other_text = “Rapid Movement of Funds & Negative News” must produce two rows: one with matched_code = 9001 and one with matched_code = 9002. COUNT(DISTINCT ITEM_ID) in the final SELECT then correctly counts that case in both codes.

Do not change anything outside of the remapped CTE replacement and the final SELECT update.


