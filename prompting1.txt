SYNTHESIZE_RULES_PROMPT = """
You are an AML expert tasked with inductively synthesizing typology rules from
extracted SAR narrative structures.

You will receive:
- A list of active FinCEN tags (each with a single code, typology label, and description)
- The current candidate rules hypothesis (may be empty on first narrative)
- A new extracted narrative structure

Your task is to return a COMPLETE, UPDATED rules hypothesis JSON.

MERGING RULES (critical):
- If the new narrative exhibits the same behavioral pattern as an existing rule
  (same channel, transaction type, and directional flow), INCREMENT that rule's
  supporting_case_count and refine its threshold. Do NOT create a new rule.
- Only create a new rule_id if the behavioral pattern is genuinely distinct
  from ALL existing rules.
- The final output must be consolidated and non-overlapping. Prefer fewer,
  stronger rules over many similar rules.

OUTPUT RULES:
- Emit exactly ONE typology_rules entry per item in the fincen_tags list.
- fincen_tag must match the provided code exactly (e.g. "821"). Never combine.
- typology must match the provided label exactly. Never invent new labels.
- rule_id format: {TYPOLOGY_PREFIX}_{FINCEN_TAG}_{sequence}, e.g. ML_821_001
  Use ML for MONEY_LAUNDERING, OSA for OTHER_SUSPICIOUS_ACTIVITY,
  STR for STRUCTURING, FRD for FRAUD, TF for TERROR_FINANCING.
- condition must be expressed as conditional logic using observable transaction
  fields, NOT narrative descriptions. Format:
  transaction.channel == 'Zelle' AND transaction.direction == 'credit'
  AND narrative.key_findings.includes('suspicion concerning source of funds')
- threshold must include a specific dollar amount and time window.
- Do not use vague language like "significant" or "large" without a dollar figure.

Return JSON only, no other text.
"""
