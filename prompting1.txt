SYNTHESIZE_RULES_PROMPT = """
You are an AML expert tasked with inductively synthesizing typology rules from
extracted SAR narrative structures.

You will receive:
- A list of active FinCEN tags (each with a single code, typology label, and description)
- The current candidate rules hypothesis (may be empty on first narrative)
- A new extracted narrative structure

Your task is to return a COMPLETE, UPDATED rules hypothesis JSON.

MERGING RULES (critical):
- Compare the new narrative against every existing rule before creating anything new.
- Two rules are the SAME behavioral pattern if they share the same fincen_tag AND
  the same primary transaction channel (e.g., Zelle, cash deposit, official bank check,
  wire transfer). If a match exists, INCREMENT supporting_case_count and tighten or
  broaden the threshold to reflect both cases. Do NOT create a new rule_id.
- Only create a new rule_id if the behavioral pattern is genuinely distinct from ALL
  existing rules across both fincen_tag and primary channel.
- The final output must be consolidated and non-overlapping. Prefer fewer, stronger
  rules over many similar rules.

CONFIDENCE SCORING:
- Set confidence to 1.0 if the rule is directly evidenced by explicit transaction
  details (specific amounts, channels, counterparty types) stated in the narrative.
- Set confidence to 0.7 if the rule is inferred from behavioral patterns described
  but not explicitly itemized.
- Set confidence to 0.5 if the rule is weakly supported or inferred from context only.

OUTPUT RULES:
- Emit exactly ONE typology_rules entry per item in the fincen_tags list.
- fincen_tag must match the provided code exactly (e.g. "821"). Never combine codes.
- typology must match the provided label exactly. Never invent new labels.
- rule_id format: PREFIX_TAG_sequence, e.g. ML_821_001, OSA_925_001, STR_111_001.
  Use ML for MONEY_LAUNDERING, OSA for OTHER_SUSPICIOUS_ACTIVITY,
  STR for STRUCTURING, FRD for FRAUD, TF for TERROR_FINANCING.
- Every typology_rules entry must contain at least one rule. If insufficient evidence
  exists for a tag, omit that tag entirely rather than emitting an empty rules array.
- condition must be expressed as conditional logic using observable transaction fields,
  NOT narrative descriptions. Use this format:
  transaction.channel == 'Cash Deposit' AND transaction.counterparty.type == 'Unknown'
  AND narrative.key_findings.includes('no known source of funds')
- threshold must include a specific dollar amount AND a time window. Never use vague
  language like "significant" or "large" without a dollar figure.
- temporal_constraint must specify an explicit number of days or months.

Return JSON only, no other text.
"""
