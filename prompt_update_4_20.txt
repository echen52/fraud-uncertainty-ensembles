Fix 1 — OR rule in SYNTHESIZE_RULES_PROMPT
Find:
 - Only create a new rule_id if the behavioral pattern is genuinely distinct from ALL
   existing rules across both fincen_tag AND primary channel.
Replace with:
 - Only create a new rule_id if the behavioral pattern is genuinely distinct from ALL
   existing rules across both fincen_tag AND primary channel.
 - OR is permitted in condition ONLY when channels are functionally equivalent payment
   mechanisms (e.g. Zelle and P2P are both peer-to-peer and may be combined).
   OR is NOT permitted to combine structurally different transaction types simply
   because they appeared in the same narrative.

Fix 2 — condition field schema in SYNTHESIZE_RULES_PROMPT
Find:
 - condition must contain ONLY observable transaction fields. No narrative text.
   Format: transaction.channel == 'Zelle' AND transaction.direction == 'Debit'
Replace with:
 - condition must contain ONLY these three permitted fields:
     transaction.channel   — the payment channel (e.g. 'Zelle', 'Cash', 'Wire Transfer')
     transaction.direction — direction of flow (e.g. 'inflow', 'outflow', 'credit', 'debit')
     transaction.amount    — dollar amount (e.g. transaction.amount >= 10000)
   No other fields are permitted. Do not invent fields like transaction.type,
   transaction.frequency, or any other field not in this list.
   Format example: transaction.channel == 'Cash' AND transaction.direction == 'inflow'


- condition must contain ONLY these three permitted fields:
     transaction.channel   — the payment channel
     transaction.direction — direction of flow (e.g. 'inflow', 'outflow', 'credit', 'debit')
     transaction.amount    — dollar amount (e.g. transaction.amount >= 10000)
   No other fields are permitted. Do not invent fields like transaction.type,
   transaction.frequency, or any other field not in this list.
   Format example: transaction.channel == 'Cash' AND transaction.direction == 'inflow'
 - transaction.channel must use ONLY these BOT package canonical values:
     'P2P'           — all peer-to-peer transfers regardless of platform
                       (Zelle, Cash App, Venmo, PayPal, NFCU P2P all map to 'P2P')
     'Cash'          — cash deposits and withdrawals
     'Wire Transfer' — domestic and international wires
     'ACH'           — automated clearing house transfers
     'Check'         — check deposits
     'ATM'           — ATM transactions
   Never use platform-specific names in condition. They belong in indicators only.
   OR is never needed in condition — P2P already covers all peer-to-peer platforms.
