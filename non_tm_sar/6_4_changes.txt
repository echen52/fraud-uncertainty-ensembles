gemini -p "You are a senior software engineer. Review the following files and identify ALL opportunities for refactoring for efficiency, cleanliness, and good SWE etiquette. Do NOT make any changes. Only report findings, organized by file.

Files to review:
- parse_narrative.py
- llm_helpers.py
- induct_by_id.py
- eval_investigations.py
- eval_hrc.py
- eval_filed_on_check.py
- eval_negative_news.py

For each file, list specific refactoring opportunities with brief justification. Also identify any cross-file consolidation opportunities, particularly among the eval_*.py files." \
@parse_narrative.py @llm_helpers.py @induct_by_id.py @eval_investigations.py @eval_hrc.py @eval_filed_on_check.py @eval_negative_news.py



gemini -p "Refactor the eval_*.py scripts into a unified evaluation framework. Implement the following structure:

1. A BaseEvaluator abstract base class with:
  - get_ground_truth_value(row) method
  - get_predicted_value(model_output) method
  - filter_rows(df) method (default: no filtering, override for special cases)

2. Concrete evaluator classes inheriting from BaseEvaluator:
  - One per field currently covered by eval_hrc.py, eval_filed_on_check.py, eval_negative_news.py, and eval_investigations.py
  - NegativeNewsEvaluator must override filter_rows() to exclude N/A ground truth rows

3. A single runner script run_evaluation.py that:
  - Parses arguments (argparse)
  - Loads ground truth CSV and model output JSON
  - Accepts a --fields flag to select which evaluators to run (default: all)
  - Iterates through selected evaluator objects
  - Calculates TP, FP, FN, TN, accuracy, precision, recall, F1 per evaluator
  - Prints consolidated report to console and outputs CSV

Do NOT delete the original eval_*.py files. Create the new framework alongside them." \
@eval_hrc.py @eval_filed_on_check.py @eval_negative_news.py @eval_investigations.py



gemini -p "Refactor parse_narrative.py with two changes only. Do NOT change any other logic:

1. Extract merge logic: Pull the metadata merging logic (combining results from main extraction, details extraction, and sample table extraction) into a standalone pure function called _merge_metadata_sources(). Call it from its original location.

2. Add docstring to extract_sample_transaction_table(): The docstring must explicitly state the heuristic rules used to identify table lines: a line is considered part of the table if it contains a date pattern AND a dollar amount AND is less than 150 characters long." \
@parse_narrative.py



gemini -p "Refactor induct_by_id.py into a thin client. Do NOT change any other logic.

1. Move the data fetching, DataFrame merging, and row-by-row processing loop out of induct_by_id.py and into a new method process_cases(self, case_ids: list) on the TypologyClassificationAgent class in agent.py.

2. Refactor induct_by_id.py to only:
  - Load the case_ids
  - Instantiate TypologyClassificationAgent
  - Call agent.process_cases(case_ids)
  - Save the returned results

3. Replace the hardcoded ENV_PATH with an environment variable or config file reference passed at runtime.

Review agent.py first to understand the existing class structure before making any changes." \
@induct_by_id.py @agent.py



gemini -p "Refactor llm_helpers.py with two changes only. Do NOT change any other logic.

1. Create a single private helper function _invoke_llm(...) that encapsulates all repeated boilerplate:
   - Setting up Agent, InMemorySessionService, and Runner
   - Executing runner.run_async
   - Accumulating streamed response text
   - Cleaning the response (stripping markdown/json wrappers)
   - Implementing the try/except block for JSON parsing with retry mechanism
   All public functions (extract_behavioral_signals, extract_investigations_metadata, etc.) should become thin wrappers that format their specific user prompt and call _invoke_llm().

2. Remove the top-level load_dotenv() call from module level. It should only be called at the application entry point, not on import." \
@llm_helpers.py




