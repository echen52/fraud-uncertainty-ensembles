In llm_helpers.py, in EXTRACT_INVESTIGATIONS_METADATA_PROMPT, find the
field definition line for `eft_wire` under FIELD DEFINITIONS and replace
it with:

     * `eft_wire`: "wire", "wire transfer", "international wire",
       "domestic wire", "outgoing wire", "incoming wire", "ACH",
       "electronic funds transfer", "EFT", "electronic payment",
       "payment transfer", "bank transfer", "online transfer",
       "book transfer", "telegraphic transfer", "SWIFT"



In llm_helpers.py, in the function extract_investigations_metadata(),
immediately after the LLM response is parsed into a dict, add the
following post-processing rule before the result is returned:

If the parsed result has filed_on.p2p == "Y" and
filed_on.eft_wire == "N", set filed_on.eft_wire = "Y".




Create two new scripts in the non_tm_sar_agent root directory:

1. list_negative_news_cases.py
   - Loads ground truth from non_tm_sar_reviews_filtered.csv
     (configurable via argument)
   - Filters to rows where 'Other Negative News' == 'Y'
   - Prints matching case_ids to console
   - Writes matching case_ids to negative_news_case_ids.txt,
     one case_id per line

2. eval_negative_news.py
   - Loads ground truth from non_tm_sar_reviews_filtered.csv
     (configurable via argument)
   - Loads model output from raw_indicator_matches_by_id.json
     (configurable via argument)
   - Filters to rows where 'Other Negative News' is non-missing
     (drop blanks/nulls)
   - Compares model negative_news against ground truth
     'Other Negative News'
   - Reports accuracy, precision, recall
   - Outputs comparison to console and optionally to a results CSV





In llm_helpers.py, in EXTRACT_DETAILS_METADATA_PROMPT, make the
following changes:

1. Under rule 2, replace the JSON STRUCTURE to contain exactly
  these keys: "negative_news", "negative_news_details"

2. Replace the entire FIELD DEFINITIONS section with:

  * `negative_news`: 'Y' or 'N'. 'Y' if the Details of Investigation
    section indicates that any internet research (LexisNexis,
    World-Check, Google, or similar) or public records search revealed
    ANY of the following regarding the subject(s) or associated
    entities, regardless of whether the analyst considered it relevant
    to the SAR filing:
      - Tax liens (state, federal, or IRS)
      - Tax warrants
      - Court judgments or civil litigation (any party)
      - Criminal charges, indictments, or convictions
      - Fraud allegations or scam associations
      - Drug or controlled substance associations
      - Any other adverse public record findings
    'N' only if the narrative explicitly states that all research
    returned no derogatory or relevant adverse findings with no
    subsequent qualifying statements.

  * `negative_news_details`: A 1-2 sentence summary of the specific
    adverse finding(s) identified, including the source (e.g.
    LexisNexis, World-Check), the nature of the finding, and any
    jurisdiction or date context mentioned in the text. Empty string
    if negative_news is 'N'.

3. Update the expected output in all three examples to include
  "negative_news_details":
  - Example 1: "negative_news_details": "LexisNexis found tax
    violations and Ohio state tax liens against the subject and their
    business; internet research identified a related company stated
    to be scamming customers."
  - Example 2: "negative_news_details": ""
  - Example 3: "negative_news_details": "LexisNexis and World-Check
    did not reveal derogatory information; however, the client has
    multiple recent State and Federal Tax Liens."



