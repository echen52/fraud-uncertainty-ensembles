In llm_helpers.py, add a new private helper function called 
_filter_relevant_investigation_paragraphs directly above 
_format_investigations_metadata_prompt.

The function signature:
def _filter_relevant_investigation_paragraphs(details_text: str) -> str:

Logic:
- Split details_text by double newline (paragraph boundary)
- Define a relevance keyword list: "suspicious", "unusual", "alert", 
  "negative news", "high-risk", "wire", "cash", "check", "ACH", "EFT", 
  "P2P", "Zelle", "Venmo", "CashApp", "bitcoin", "crypto", "ethereum", 
  "jurisdiction", any country name from the HIGH-RISK COUNTRIES LIST
- Keep a paragraph if it contains at least one keyword (case-insensitive match)
- Join and return the filtered paragraphs as a single string
- If no paragraphs match, return the first two paragraphs as fallback

Then in _format_investigations_metadata_prompt, add:

filtered_details = _filter_relevant_investigation_paragraphs(
    narrative_sections.get('details_of_investigation', '')
)

And add to the return string after CONCLUSION:

DETAILS OF INVESTIGATION (RELEVANT EXCERPTS):
{filtered_details}





import os
os.chdir("/home/jupyter/custom_agent/typology_agent")

from tca.llm_helpers import _filter_relevant_investigation_paragraphs

# Paste a real Details of Investigation text from an actual case
test_details = """
<paste a real multi-paragraph Details of Investigation block here>
"""

result = _filter_relevant_investigation_paragraphs(test_details)
print("=== FILTERED OUTPUT ===")
print(result)
print(f"\n=== PARAGRAPH COUNT: {len(result.split(chr(10)*2))} ===")




In tca/llm_helpers.py, in _filter_relevant_investigation_paragraphs, 
replace the KEYWORDS list with two separate lists and update the logic:

INVESTIGATIVE_KEYWORDS = [
    "suspicious activity", "suspicious", "unusual", "alert",
    "negative news", "high-risk", "jurisdiction", "appears excessive",
    "unknown source", "no clear purpose", "deemed suspicious",
    "considered suspicious", "rapidly moved", "no legitimate"
]

HIGH_RISK_COUNTRIES = [
    "afghanistan", "belarus", "central african republic", "congo (drc)",
    "cuba", "ethiopia", "iran", "iraq", "libya", "mali", "myanmar (burma)",
    "nicaragua", "north korea", "russia", "somalia", "sudan", "south sudan",
    "syria", "ukraine (crimea/dnr/lnr)", "venezuela", "yemen"
]

Keep a paragraph only if it matches at least one term from 
INVESTIGATIVE_KEYWORDS OR at least one country from HIGH_RISK_COUNTRIES.
Remove instrument keywords (cash, check, wire, p2p, zelle, etc.) 
from the matching logic entirely.





python3 -c "
import sys, os
sys.path.insert(0, os.path.abspath('.'))
from tca.tools.sar_narrative_tools import get_sar_narratives_for_ids
from tca.parse_narrative import split_narrative_sections
df = get_sar_narratives_for_ids(['AML_INV_00000050085'])
sections = split_narrative_sections(df.iloc[0].get('NARRATIVE'))
details = sections.get('details_of_investigation', '')
print(repr(details[:500]))
"




In tca/llm_helpers.py, in _filter_relevant_investigation_paragraphs,
change the split line from:

    paragraphs = details_text.split('\n\n')

to:

    import re
    paragraphs = re.split(r'\r?\n\r?\n', details_text)

This handles both \r\n\r\n and \n\n delimiters robustly.
Keep all other logic unchanged.





In tca/llm_helpers.py, replace the entire body of 
_filter_relevant_investigation_paragraphs with this logic:

1. Split details_text using re.split(r'\r?\n\r?\n', details_text)
2. Strip each paragraph
3. EXCLUDE any paragraph where the lowercased text contains 
   "no suspicious activity"
4. Join remaining paragraphs with '\n\n' and return
5. Fallback: if all paragraphs are excluded, return the full 
   details_text unchanged



In tca/llm_helpers.py, in EXTRACT_INVESTIGATIONS_METADATA_PROMPT,
update both few-shot examples to include a DETAILS OF INVESTIGATION 
section between CONCLUSION and EXPECTED OUTPUT.

For Example 1, add after CONCLUSION:

DETAILS OF INVESTIGATION:
Suspicious activity being reported in this account consists of large 
cash deposits inconsistent with the customer's profile. Funds were 
subsequently wired to Iran. The customer had no prior relationship 
with any Iranian entities. The source of funds is unknown.

For Example 2, add after CONCLUSION:

DETAILS OF INVESTIGATION:
The suspicious activity identified within this account consists of 
P2P transactions through various platforms. No high-risk countries 
were identified. No prior alerts were generated on this account.

Also update the NARRATIVE SECTIONS header block in 
_format_investigations_metadata_prompt to include:

DETAILS OF INVESTIGATION:
{narrative_sections.get('details_of_investigation', 'Not found')}

after the CONCLUSION section




In GEMINI.md, make the following changes:

1. Under "## Investigations Extraction – Use Case 2", replace the 
  entire "### TODO (ordered)" section with:

### TODO (ordered)
1. **Build eval framework** — create eval_investigations.py at root 
  level of non_tm_sar_agent.
  - Ground truth source: configurable, default = 
    non_tm_transaction_sar_excel.csv
  - Model output source: configurable, default = 
    raw_indicator_matches_by_id.json
  - Compares all four extraction fields: filed_on (check, cash, 
    eft_wire, p2p, virtual_currency), hrc flag, 
    alerts_prior_to_sar, negative_news
  - Reports per-field accuracy, precision, recall
  - Outputs to console + optional results CSV

2. **Run eval** — Introduction+Conclusion only vs 
  Introduction+Conclusion+Details of Investigation. Determine 
  better input configuration before further prompt iteration.

3. **Improve prompt quality (pending eval results)** — semantic 
  concept definitions for eft_wire, p2p, virtual_currency; 
  replace synthetic few-shot examples with real anonymized 
  SAR narratives including edge cases.

2. In "### WORKING" section, add:
  - extract_investigations_metadata() — LLM call in llm_helpers.py,
    extracts filed_on, hrc, alerts_prior_to_sar, negative_news.
    Currently uses Introduction + Conclusion only.
  - _filter_relevant_investigation_paragraphs() — exists in 
    llm_helpers.py, splits on \r?\n\r?\n. Not active pending eval.

3. Delete the duplicate "## Gemini CLI Notes" block 
  (second instance starting around line 337).



