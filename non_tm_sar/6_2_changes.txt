In the non_tm_sar_agent/ directory, search all Python files for any code that generates or writes a "spacy_frames" key into the output JSON (raw_indicator_matches_by_id.json). 

1. Find every reference to "spacy_frames" across all .py files in non_tm_sar_agent/ and its subdirectories.
2. Identify the function responsible for populating it and which file it lives in.
3. Report back the file name, function name, and line numbers where "spacy_frames" is written or assembled — do not make any changes yet.



In eval_investigations.py, make the following two changes:

1. Fix the HRC field mapping. Find where the eval script defines or iterates 
   the list of fields to evaluate. The correct path for HRC is 
   investigations_metadata -> hrc -> flag. Remove any separate "hrc" row 
   that is not accessing the nested flag key. There should be exactly one 
   HRC row in the output, evaluated as "hrc.flag" using the path 
   investigations_metadata["hrc"]["flag"].

2. Remove alerts_prior_to_sar from evaluation entirely. Comment out (do not 
   delete) any field definition, mapping, or evaluation logic related to 
   alerts_prior_to_sar.

Do not modify any other field mappings or eval logic.




In llm_helpers.py and parse_narrative.py, make the following changes. Read both files fully before making any changes.

PART 1 — llm_helpers.py: Modify EXTRACT_DETAILS_METADATA_PROMPT.

Add "hrc" as an additional key to the JSON structure. The updated prompt must:

1. Change the JSON STRUCTURE rule to require exactly these keys:
   "negative_news", "negative_news_details", "hrc"

2. Add the following HRC field definition under FIELD DEFINITIONS, 
   identical in definition to the one in EXTRACT_INVESTIGATIONS_METADATA_PROMPT:
   * hrc: An object with keys "flag" (Y/N) and "jurisdictions" (list of strings).
     - "flag": "Y" if any high-risk country is mentioned, "N" otherwise.
     - "jurisdictions": A list of the specific high-risk country names mentioned 
       in the Details of Investigation section. If no high-risk countries are 
       mentioned, this list should be empty.
     - Use the same HIGH-RISK COUNTRIES LIST already present in 
       EXTRACT_INVESTIGATIONS_METADATA_PROMPT. Copy it verbatim into this prompt.

3. Add a corresponding example to the existing examples showing hrc in the 
   expected output. Use a simple case: Details of Investigation mentions 
   a wire to Nigeria. Expected: hrc: {"flag": "Y", "jurisdictions": ["Nigeria"]}

Do not modify EXTRACT_INVESTIGATIONS_METADATA_PROMPT or any other prompt.

PART 2 — parse_narrative.py: After the existing extract_details_metadata() call 
and before the final merge (investigations_metadata.update(details_metadata)), 
add HRC augmentation logic:

    # HRC augmentation from details_of_investigation (only if Call 1 missed it)
    details_hrc = details_metadata.pop("hrc", None)
    if details_hrc and investigations_metadata.get("hrc", {}).get("flag") == "N":
        if details_hrc.get("flag") == "Y":
            investigations_metadata["hrc"] = details_hrc

The .pop() removes "hrc" from details_metadata before the .update() call so 
it does not get overwritten by the general merge. The augmentation only applies 
when Call 1 returned flag == "N" and Call 2 found flag == "Y".

Do not modify extract_details_metadata() function itself. Do not modify the 
existing merge line (investigations_metadata.update(details_metadata)).



In llm_helpers.py, in BOTH EXTRACT_INVESTIGATIONS_METADATA_PROMPT and 
EXTRACT_DETAILS_METADATA_PROMPT, find the jurisdictions field definition 
and replace it with:

- "jurisdictions": A list of the specific high-risk country name(s) 
  explicitly mentioned in the narrative. This field is MANDATORY when 
  flag is "Y" — if flag is "Y" and this list is empty, that is an error. 
  Scan the entire provided text and extract every country name that matches 
  the HIGH-RISK COUNTRIES LIST. If no high-risk countries are mentioned, 
  return an empty list.

Do not modify anything else in either prompt.



In llm_helpers.py, in BOTH EXTRACT_INVESTIGATIONS_METADATA_PROMPT and 
EXTRACT_DETAILS_METADATA_PROMPT, add the following clarifying instruction 
immediately after the jurisdictions field definition:

  IMPORTANT: Phrases like "KeyBank designated high risk country", "KeyBank 
  High Risk Country designation [country]", or "high risk country 
  jurisdiction [country]" explicitly identify a high-risk country. Extract 
  the country name that follows these phrases into the jurisdictions list. 
  Do not skip a country because it appears in an institutional label or 
  designation phrase.

Do not modify anything else.







Write a new Python script called export_to_csv.py in the non_tm_sar_agent/ 
directory. This script converts raw_indicator_matches_by_id.json to a flat 
CSV that mirrors the ground truth format.

SCRIPT REQUIREMENTS:

1. Input: output/raw_indicator_matches_by_id.json
   Output: output/model_output_flat.csv
   Both paths configurable via argparse:
   --input (default: output/raw_indicator_matches_by_id.json)
   --output (default: output/model_output_flat.csv)

2. One row per case. Output columns in this exact order:
   CASE_ID
   CASE_TITLE (leave blank — not in JSON)
   CASE_OPEN_DT (from case_create_dt)
   CASE_CLOSED_DT (from case_closed_dt)
   CASE_TYPE
   CASE_SOURCE
   SAR_AMT (from sar_amt)
   Check (from investigations_metadata.filed_on.check)
   Cash (from investigations_metadata.filed_on.cash)
   EFTWire (from investigations_metadata.filed_on.eft_wire)
   P2P (from investigations_metadata.filed_on.p2p)
   VirtualCurrency (from investigations_metadata.filed_on.virtual_currency)
   HRC (from investigations_metadata.hrc.flag)
   HRC_Jurisdictions (from investigations_metadata.hrc.jurisdictions — 
     join list as semicolon-separated string)
   NegativeNews (from investigations_metadata.negative_news)
   NegativeNewsDetails (from investigations_metadata.negative_news_details)
   ReviewPeriodStart (from review_period.start)
   ReviewPeriodEnd (from review_period.end)
   SuspiciousTotal (from suspicious_total)
   FincenTags (from active_fincen_tags — join list of fincen_tag values 
     as semicolon-separated string)

3. For any missing/null field, write empty string. Do not crash on missing keys 
   — use .get() with empty string defaults throughout.

4. alerts_prior_to_sar: exclude entirely.

5. Print to console on completion: 
   "Exported {N} cases to {output_path}"


