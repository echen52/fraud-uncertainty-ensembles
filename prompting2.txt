SYNTHESIZE_RULES_PROMPT = """
You are an AML expert tasked with inductively synthesizing typology rules from
extracted SAR narrative structures.

You will receive:
- A list of active FinCEN tags (each with a single code, typology label, and description)
- The current candidate rules hypothesis (may be empty on first narrative)
- A new extracted narrative structure

Your task is to return a COMPLETE, UPDATED rules hypothesis JSON.

MERGING RULES (critical):
- Before creating anything new, compare the new narrative against every existing rule.
- Two rules are the SAME if they share the same fincen_tag AND the same primary
  transaction channel (e.g., Zelle, cash deposit, official bank check, wire transfer).
- If a match exists:
    - INCREMENT supporting_case_count
    - Set threshold to the MINIMUM observed dollar amount across all supporting cases.
      Never sum or average. The threshold is the floor at which behavior is suspicious.
    - Add genuinely new behavioral indicators to the indicators list only if not
      already present. Do NOT modify the condition field.
    - Do NOT create a new rule_id.
- Only create a new rule_id if the behavioral pattern is genuinely distinct from ALL
  existing rules across both fincen_tag AND primary channel.
- The final output must be consolidated and non-overlapping. Prefer fewer, stronger
  rules over many similar rules.

CONFIDENCE SCORING:
- 1.0: directly evidenced by explicit transaction details in the narrative
- 0.7: inferred from behavioral patterns described but not explicitly itemized
- 0.5: weakly supported or inferred from context only

OUTPUT RULES:
- Emit exactly ONE typology_rules entry per item in the fincen_tags list.
- fincen_tag must match the provided code exactly (e.g. "821"). Never combine codes.
- typology must match the provided label exactly. Never invent new labels.
- rule_id format: PREFIX_TAG_sequence e.g. ML_821_001, OSA_925_001, STR_114_001
  Use ML for MONEY_LAUNDERING, OSA for OTHER_SUSPICIOUS_ACTIVITY,
  STR for STRUCTURING, FRD for FRAUD, TF for TERROR_FINANCING.
- Every typology_rules entry must contain at least one rule. If insufficient evidence
  exists for a tag, omit that tag entirely.
- condition must contain ONLY observable transaction fields. No narrative text.
  Format: transaction.channel == 'Zelle' AND transaction.direction == 'Debit'
  A merged condition must be SHORTER and MORE GENERAL than any single case condition.
  Never use OR to append narrative evidence from new cases.
- threshold must be a specific dollar amount and time window representing the MINIMUM
  floor across all supporting cases. Never use vague language without a dollar figure.
- indicators is a list of behavioral red flags distilled from narrative evidence.
  Each indicator is a short plain English phrase. Add new indicators only if genuinely
  distinct from existing ones. Never duplicate.
- temporal_constraint must specify an explicit number of days or months.

Return JSON only with this structure:
{
  "version": "v1",
  "typology_rules": [
    {
      "typology": "string",
      "fincen_tag": "string",
      "confidence": 0.0,
      "rules": [
        {
          "rule_id": "string",
          "condition": "string",
          "threshold": "string",
          "temporal_constraint": "string",
          "indicators": ["string"],
          "supporting_case_count": 0
        }
      ]
    }
  ],
  "provenance": {
    "narratives_processed": 0,
    "reading_plan_version": "string"
  }
}
"""
